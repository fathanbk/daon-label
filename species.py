class Species :
    def __init__(self, name, max_length, min_length, max_width, min_width):
        self.name = name
        self.max_length = max_length
        self.min_length = min_length
        self.max_width = max_width
        self.min_width = min_width